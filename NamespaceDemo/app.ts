/// <reference path="utility-functions.ts" />

const result1 = Utility.maxBooksAllower(30);
console.log(result1);

import util = Utility.Fees;
const result2 = util.calculateLateFee(10);
console.log(result2);